---
description: 新建页面与浮层默认采用浅色模式（微信式浅灰底 + 白卡片）
alwaysApply: true
---

# UI 默认浅色

在本仓库新增**全屏页、抽屉、弹层、菜单、表单页**时，默认使用**浅色模式**：

- 页面/区域背景：`#ededed`、`#f7f7f7` / `#f5f5f5`，或与现有全屏聊天、会话抽屉一致。
- 卡片、浮层、输入条：**白底** `#ffffff`，细描边 `#e7e7e7` / `#e8e8e8`，主文案 `#191919`，次要 `#888`、`#b2b2b2`。
- 阴影：低对比 `rgba(0,0,0,0.06)`～`0.12`。
- 除非明确要求暗色主题，避免纯黑主容器（如 `#1f1f1f`）作为列表主背景。
